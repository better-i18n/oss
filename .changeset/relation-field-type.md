---
"@better-i18n/mcp-content": minor
---

Add `relation` field type for linking entries across content models (belongs_to).
Add generic `fieldConfig` object to field definitions for type-specific configuration.
