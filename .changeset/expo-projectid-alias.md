---
"@better-i18n/expo": minor
---

`initBetterI18n` and `BetterI18nBackend` now accept `projectId` as the canonical project field, matching the "Project ID" shown in the dashboard and the Content SDK. `project` keeps working as a deprecated alias.
