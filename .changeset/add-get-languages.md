---
"@better-i18n/expo": minor
---

Add `getLanguages()` — a synchronous function that returns the languages resolved during `initBetterI18n()`. This eliminates the need for consumers to cache language data in React state.
