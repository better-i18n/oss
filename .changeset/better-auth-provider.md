---
"@better-i18n/server": minor
---

Add Better Auth localization provider at `@better-i18n/server/providers/better-auth`. Translates auth error messages via CDN — add translations from the dashboard, no redeployment needed.
